import { SelectedProductPipe } from './selected-product.pipe';

describe('SelectedProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedProductPipe();
    expect(pipe).toBeTruthy();
  });
});
